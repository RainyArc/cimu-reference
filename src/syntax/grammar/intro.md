# Grammar

This chapter describes the syntactic grammar of Cimu.

Cimu has two kinds of code:

- regular code, used for declarations, statements, and expressions;
- scene code, used by visual novel scenes and containing a few scene-only
  constructs.

Scene-only constructs, such as text, save points, and labels, are only valid in
the top-level scope of a scene unless otherwise specified.
